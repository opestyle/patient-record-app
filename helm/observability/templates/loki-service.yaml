apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: loki
  ports:
    - protocol: TCP
      port: {{ .Values.loki.servicePort }}
      targetPort: {{ .Values.loki.servicePort }}
